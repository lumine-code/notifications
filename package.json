{
  "name": "notifications",
  "author": "lumine-code",
  "version": "1.0.0",
  "description": "A tidy way to display notifications.",
  "keywords": [
    "alert",
    "message",
    "toast",
    "warning",
    "error"
  ],
  "repository": "https://github.com/lumine-code/notifications",
  "bugs": {
    "url": "https://github.com/lumine-code/notifications/issues"
  },
  "license": "MIT",
  "main": "./lib/main",
  "files": [
    "keymaps",
    "lib",
    "spec",
    "styles"
  ],
  "engines": {
    "lumine": "^1.0.0"
  },
  "backgroundTips": [
    "{% if keys['notifications:toggle-log'] %}You can reread every notification you have dismissed with {{ 'notifications:toggle-log' | keystroke }}{% else %}Every notification you dismiss is kept in the notifications log, so nothing is lost.{% endif %}"
  ],
  "scripts": {
    "test": "lumine --test spec",
    "lint": "eslint . --max-warnings 0",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "configSchema": {
    "showErrorsInDevMode": {
      "title": "Show Errors In Dev Mode",
      "description": "Show notifications for uncaught exceptions even if Lumine is running in dev mode. If this config setting is disabled, uncaught exceptions will trigger the dev tools to open and be logged in the console tab.",
      "type": "boolean",
      "default": false
    },
    "defaultTimeout": {
      "title": "Default Timeout",
      "description": "The default notification timeout for a non-dismissable notification.",
      "type": "integer",
      "minimum": 1000,
      "default": 5000
    }
  },
  "deserializers": {
    "notifications/NotificationsLog": "createLog"
  },
  "dependencies": {
    "@lumine-code/fs-plus": "github:lumine-code/fs-plus#ff8553b5ea456db3545112beccd6129fdcae7db3",
    "semver": "^7.8.5",
    "stacktrace-parser": "^0.1.11"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@lumine-code/temp": "github:lumine-code/node-temp#5d0a30ffb4d68f21577d057aef8844a55479e304",
    "coffeelint": "^2.1.0",
    "eslint": "^10.8.0",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-n": "^18.2.2",
    "globals": "^17.9.0",
    "prettier": "^3.9.6"
  }
}
